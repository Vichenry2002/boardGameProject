package ca.mcgill.splendorserver.gameelements;

/**
 * Enum for the colors of the tokens, not used yet, might be useful later.
 */
public enum TokenColor {
  /**
   * Have to add a full comment header section to each possible enum value because the provided
   * version of the javadoc plugin has stupid rules, and we are not allowed to change it.
   */
  WHITE,
  /**
   * Read above.
   */
  BLUE,
  /**
   * Read above.
   */
  GREEN,
  /**
   * Read above.
   */
  RED,
  /**
   * Read above.
   */
  BLACK,
  /**
   * Read above.
   */
  GOLD
}
